#ifndef UTILS_HPP
#define UTILS_HPP

#include "Tree.hpp"
#include "Graph.hpp"


class Utils
{
public:
    Utils();
    static void display_tree(Tree * tree);
    static void display_graph(Graph * graph);

};

#endif // UTILS_HPP
